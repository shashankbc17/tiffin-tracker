# GitHub Pages Deployment Rule

- **MANDATORY**: Whenever you make changes or fixes, ALWAYS commit and push all changes to GitHub Pages (`git push origin main` and/or `npm run deploy`) so the user can verify changes live on GitHub Pages.
- Ensure the production build passes before pushing.
